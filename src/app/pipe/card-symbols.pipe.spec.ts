import { CardSymbolsPipe } from './card-symbols.pipe';

describe('CardSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new CardSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
