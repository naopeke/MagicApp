import { PriceEuroPipe } from './price-euro.pipe';

describe('PriceEuroPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceEuroPipe();
    expect(pipe).toBeTruthy();
  });
});
